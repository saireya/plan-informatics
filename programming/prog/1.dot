seqdiag
{
	span_height = 0;
	activation = none;
	default_fontsize = 12;
	group { T [label="Teacher"]; color = "#ffcccc"; }
	group { S [label="Student"]; color = "#ccffcc"; }
	group { PC[label="PC"];      color = "#ccccff"; }

	S -> PC [label="PCを起動する"]
	S --> T [label="PCの起動を確認する"]

	=== 導入(5分) ===
	T -> S [label="本時の内容を伝える"]
	S --> T [label="Squeakを導入したか確認する"]

	=== 展開: オブジェクトの作成(15分) ===
	T => S [label="Squeakの起動方法を説明する", return="Squeakの起動を確認する"]
	{
		S -> PC [label="Squeakを起動する"]
	}
	T => S [label="オブジェクトの作り方を説明する", return="オブジェクトの作成を確認する"]
	{
		S -> PC [label="車のオブジェクトを作る", leftnote="塗りつぶした部分がオブジェクトとなる\nので、背景を塗らないよう注意する"]
	}

	=== 展開: オブジェクトの操作(15分) ===
	T => S [label="オブジェクトに対して行える命令を説明する", return="逐次処理を理解する"]
	{
		S => PC [label="各種命令を実行する", return="車が動くことを確認する"]
		S => PC [label="命令を並べたスクリプトを作る", return="逐次処理を実行する", leftnote="スクリプトに並べた命令は、並列でなく\n直列に実行されることを説明する"]
	}
	T => S [label="スクリプトを反復実行する方法を説明する", return="反復処理を理解する"]
	{
		S => PC [label="スクリプトを反復実行する", return="反復処理を実行する"]
	}

	=== 展開: オブジェクト間のメッセージ交換(10分) ===
	T => S [label="オブジェクト間のメッセージ交換を学ぶことを伝える", return="メッセージ交換に基づくプログラミング手法を理解する"]
	{
		S -> PC [label="ハンドルのオブジェクトを作る"]
		S => PC [label="ハンドルの向きに応じて車が曲がるよう、スクリプトを修正する", return="オブジェクト間通信を実行する"]
	}

	=== 展開: プロジェクトの保存(5分) ===
	T => S [label="プロジェクトの保存方法を伝える", return="プロジェクトの保存を確認する"]
	{
		S -> PC [label="作成中のプロジェクトを保存する", leftnote="休憩中に問題が生じないよう、\n1時限目の最後に行う"]
	}
}
